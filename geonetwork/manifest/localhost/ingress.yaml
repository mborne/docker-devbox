apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: geonetwork
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`geonetwork.dev.localhost`)
      kind: Rule
      services:
        - name: geonetwork
          port: 8080
